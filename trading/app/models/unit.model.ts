export interface Settings {
    start_value: number;
    start_date: number[];
    end_date: number[];
    allocation: number[];
    target_stocks: string[];
    benchmark: string;
}

export interface Metadata {

}